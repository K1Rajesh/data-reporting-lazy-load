export interface SideNavListDataModel{
    id:number,
    name: string,
    hasSubMenu:boolean,
    showSubMenu:boolean,
    isEnabled:boolean
}

export type SideNavListDTModel= {
  id:number,
  name: string,
  hasSubMenu:boolean,
  showSubMenu:boolean,
  isEnabled:boolean
}